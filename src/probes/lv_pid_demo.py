# Baseline LV + PID on predation β(t), recording eigenvalue flow and flips
# Mirrors your minimal test triplet (baseline, fixed-lag perturb, rollback).
# (Skeleton only; fill with your preferred ODE integrator)
